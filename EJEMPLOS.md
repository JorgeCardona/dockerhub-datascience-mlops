# Referencia de Sintaxis por Kernel (JupyterLab)

| Lenguaje / Kernel | Código de Ejemplo (con Versión) |
| :--- | :--- |
| **Python (ML - Data Science)** | import sys<br>print(f"Python version: {sys.version}")<br><br>def calculate_square(number: int) -> int:<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number<br><br>result = calculate_square(5)<br>print(f"Square of 5: {result}") |
| **Bash** | echo "Bash version: \$BASH_VERSION"<br><br>calculate_square() {<br>&nbsp;&nbsp;&nbsp;&nbsp;local number=\$1<br>&nbsp;&nbsp;&nbsp;&nbsp;echo \$((number * number))<br>}<br><br>result=\$(calculate_square 5)<br>echo "Square of 5: \$result" |
| **C++ 20** | #include &lt;iostream&gt;<br><br>std::cout &lt;&lt; "C++ Standard: " &lt;&lt; __cplusplus &lt;&lt; std::endl;<br><br>auto calculate_square = [](int number) {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number;<br>};<br><br>int result = calculate_square(5);<br>std::cout &lt;&lt; "Square of 5: " &lt;&lt; result &lt;&lt; std::endl; |
| **Deno (TS / JS)** | console.log(`Deno version: \${Deno.version.deno}`);<br><br>function calculateSquare(number: number): number {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number;<br>}<br><br>const result = calculateSquare(5);<br>console.log(`Square of 5: \${result}`); |
| **Elixir** | IO.puts("Elixir version: #{System.version()}")<br><br>calculate_square = fn number -&gt; number * number end<br><br>result = calculate_square.(5)<br>IO.puts("Square of 5: #{result}") |
| **Go (Gophernotes)** | import ("fmt"; "runtime")<br><br>fmt.Printf("Go version: %s\n", runtime.Version())<br><br>func calculateSquare(number int) int {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number<br>}<br><br>result := calculateSquare(5)<br>fmt.Printf("Square of 5: %d\n", result) |
| **Java (IJava)** | System.out.println("Java version: " + System.getProperty("java.version"));<br><br>int calculateSquare(int number) {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number;<br>}<br><br>int result = calculateSquare(5);<br>System.out.println("Square of 5: " + result); |
| **JavaScript (Node.js)** | console.log(`Node.js version: \${process.version}`);<br><br>function calculateSquare(number) {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number;<br>}<br><br>const result = calculateSquare(5);<br>console.log(`Square of 5: \${result}`); |
| **Julia** | println("Julia version: ", VERSION)<br><br>function calculate_square(number::Int)<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number<br>end<br><br>result = calculate_square(5)<br>println("Square of 5: \$result") |
| **Kotlin** | println("Kotlin version: \${KotlinVersion.CURRENT}")<br><br>fun calculateSquare(number: Int): Int {<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number<br>}<br><br>val result = calculateSquare(5)<br>println("Square of 5: \$result") |
| **MATLAB / Octave** | fprintf('MATLAB/Octave version: %s\n', version);<br><br>function sq = calculate_square(number)<br>&nbsp;&nbsp;&nbsp;&nbsp;sq = number * number;<br>end<br><br>result = calculate_square(5);<br>fprintf('Square of 5: %d\n', result); |
| **R** | cat(sprintf("R version: %s\n", R.version.string))<br><br>calculate_square &lt;- function(number) {<br>&nbsp;&nbsp;&nbsp;&nbsp;return(number * number)<br>}<br><br>result &lt;- calculate_square(5)<br>cat(sprintf("Square of 5: %d\n", result)) |
| **Ruby** | puts "Ruby version: #{RUBY_VERSION}"<br><br>def calculate_square(number)<br>&nbsp;&nbsp;&nbsp;&nbsp;number * number<br>end<br><br>result = calculate_square(5)<br>puts "Square of 5: #{result}" |
| **Rust (Evcxr)** | println!("Rust Execution Context (Evcxr)");<br><br>fn calculate_square(number: i32) -&gt; i32 {<br>&nbsp;&nbsp;&nbsp;&nbsp;number * number<br>}<br><br>let result = calculate_square(5);<br>println!("Square of 5: {}", result); |
| **Scala (Almond)** | println(s"Scala version: \${util.Properties.versionString}");<br><br>def calculateSquare(number: Int): Int = {<br>&nbsp;&nbsp;&nbsp;&nbsp;number * number<br>}<br><br>val result = calculateSquare(5)<br>println(s"Square of 5: \$result") |
| **SoS (Multi-language)** | # %set python3<br>import sys<br>print(f"SoS Python Kernel version: {sys.version}")<br><br>def calculate_square(number):<br>&nbsp;&nbsp;&nbsp;&nbsp;return number * number<br><br>result = calculate_square(5)<br>print(f"Square of 5: {result}") |